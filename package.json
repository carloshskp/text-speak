{
  "name": "text-reader",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i src/tailwind.css -o assets/tailwind.min.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.9"
  }
}
